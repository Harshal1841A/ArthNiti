version: "3.9"

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite+aiosqlite:///./data/arthniti.db
      - FRONTEND_URL=http://localhost:5173
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - GROQ_API_KEY=${GROQ_API_KEY}
      - BHASHINI_API_KEY=${BHASHINI_API_KEY}
      - FIU_PRIVATE_KEY_JWK=${FIU_PRIVATE_KEY_JWK}
    volumes:
      - ./data:/app/data
      - ./models:/app/models
    command: uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload

  # Uncomment for PostgreSQL (recommended for production)
  # db:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_USER: arthniti
  #     POSTGRES_PASSWORD: arthniti
  #     POSTGRES_DB: arthniti
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data
  #
  # volumes:
  #   pgdata:
